import 'package:flutter/material.dart';

/// Admin dashboard home — implement in Block 6.
class DashboardHome extends StatelessWidget {
  const DashboardHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Offline Sewa — Admin')),
      body: const Center(child: Text('Block 6: build me')),
    );
  }
}
