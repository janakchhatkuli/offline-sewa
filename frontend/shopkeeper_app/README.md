# Shopkeeper App

Flutter mobile app for shopkeepers. Scans customer QRs and sends SMS confirmation.

```bash
flutter create . --project-name shopkeeper_app --platforms=android,ios
flutter pub get
flutter run
```
