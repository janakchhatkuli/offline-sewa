import 'package:flutter/material.dart';

/// Shopkeeper home — implement in Block 5.
class ShopkeeperHome extends StatelessWidget {
  const ShopkeeperHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Offline Sewa — Shopkeeper')),
      body: const Center(child: Text('Block 5: build me')),
    );
  }
}
